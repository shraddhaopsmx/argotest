apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-test1
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: argocd-test1
  template:
    metadata:
      labels:
        app: argocd-test1
    spec:
      containers:
      - image: ks-guestbook-demo:0.2
        name: argocd-test1
        ports:
        - containerPort: 80
