apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: pure-gitops-analysisjk
spec:
  args:
    - name: canary-hash
    - name: baseline-hash
  metrics:
  - name: pure-gitops-analysisjk
#     count: 1
#     interval: 5m
#     successCondition: result[0] >= 10
#     failureLimit: 1
#     provider:
#       plugin:
#         prometheus:
#           address: https://prometheus.autopilot.opsmx.com/
#           query: |
#             avg(rate(container_memory_usage_bytes{namespace="default"}[1m]))  
    - name: pure-gitops-analysisjk
      initialDelay: 20s
      provider:
        plugin:
          opsmx:
            address : "https://isd-dev.argo-dev.opsmx.net/"
