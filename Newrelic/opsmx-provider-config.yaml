apiVersion: v1
kind: ConfigMap
metadata:
  name: opsmx-provider-config-ss
data:
  providerConfig: |
    application: opsmx-job-ta
    lifetimeMinutes: 3
    intervalTime: 3
    gitops: true
    passScore: 90
    serviceList: 
    - metricScopeVariables: "${namespace_key},${pod_key},${app_name}"
      baselineMetricScope: "argocd,{{env.STABLE_POD_HASH}},demoapp-issuegen"
      canaryMetricScope: "argocd,{{env.LATEST_POD_HASH}},demoapp-issuegen"
      metricTemplateName: "metricym"
