apiVersion: v1
kind: ConfigMap
metadata:
  name: elastic
data:
  elastic: |
    monitoringProvider: ELASTICSEARCH
    accountName: TesitngESAonaEnv
    index: kubernetes*
    filterKey: kubernetes.pod_name
    responseKeywords: log
